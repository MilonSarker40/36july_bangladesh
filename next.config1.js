/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['en', 'bn'], // ইংরেজি এবং বাংলা
    defaultLocale: 'en',  // ডিফল্ট ভাষা ইংরেজি
  },
};

export default nextConfig;
